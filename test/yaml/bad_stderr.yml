programs:
    invalid_stderr:
        cmd: echo bad stderr
        stderr: / #<===== bad stderr

    invalid_command_in_closed_stderr:
        cmd: ./test/script/bad_stderr # must not be print since its in stderr
        stderr: "-"

    valid_stdout_with_closed_stderr:
        cmd: echo "its not stderr, it will be printed"
        stderr: "-"

