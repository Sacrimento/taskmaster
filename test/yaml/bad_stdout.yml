programs:
    stdout:
        cmd: echo bad stdout
        stdout: /

    stdout:
        cmd: echo Must not be print since its echo
        stdout: "-"
    stdout:
        cmd: mkdir / # must be print since its stderr
        stdout: "-"
