programs:
    invalid_stdout:
        cmd: echo bad stdout
        stdout: /

    echo_in_closed_stdout:
        cmd: echo "Must not be print since its echo"
        stdout: "-"

    stderr_with_closed_stdout:
        cmd: mkdir / # must be print since its stderr
        stdout: "-"
