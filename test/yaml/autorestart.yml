programs:
    unexpected:
        cmd: echo unexpected
        autostart: true
        autorestart: unexpected
        startretries: 10

    always:
        cmd: echo always
        autostart: true
        autorestart: always

    never:
        cmd: echo never
        autostart: true
        autorestart: never
